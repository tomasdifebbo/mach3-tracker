{
  "name": "mach3-backend-root",
  "version": "1.0.0",
  "description": "Root package config for Auto-Deploy on Railway",
  "scripts": {
    "install": "cd server && npm install",
    "build": "cd server && npm install",
    "start": "cd server && node server.js"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
